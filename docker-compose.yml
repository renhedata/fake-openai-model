services:
  app:
    build:
      context: .
      target: runner
    environment:
      - PORT=3000
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    healthcheck:
      test:
        [
          "CMD",
          "bun",
          "-e",
          "const r = await fetch('http://localhost:3000/health'); if (!r.ok) process.exit(1);",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
